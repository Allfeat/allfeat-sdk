{
  "name": "ats-cert-parser",
  "version": "0.1.0",
  "description": "ATS certificate parser WASM module",
  "main": "pkg/ats_cert_parser.js",
  "types": "pkg/ats_cert_parser.d.ts",
  "files": [
    "pkg"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "build:node": "wasm-pack build --target nodejs --out-dir pkg-node",
    "build:bundler": "wasm-pack build --target bundler --out-dir pkg-bundler",
    "test": "wasm-pack test --headless --firefox"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.0"
  },
  "keywords": [
    "wasm",
    "ats",
    "certificate",
    "parser"
  ],
  "author": "Allfeat labs",
  "license": "GPL-3.0-only"
}