{
    "name": "allfeat-sdk",
    "version": "0.1.0",
    "description": "Allfeat SDK - A comprehensive toolkit for blockchain music metadata",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@10.0.0",
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm -r dev",
        "test": "pnpm -r test",
        "lint": "pnpm -r lint",
        "clean": "pnpm -r clean",
        "typecheck": "pnpm -r typecheck"
    },
    "devDependencies": {
        "typescript": "^5.9.0",
        "@types/node": "^24.2.0"
    },
    "engines": {
        "node": ">=20.0.0",
        "pnpm": ">=8.0.0"
    }
}
